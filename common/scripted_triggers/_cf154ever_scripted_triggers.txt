########################################################################
# Скриптовые триггеры для всего остального
########################################################################

check_if_only_one_china_left = {
	if = {
		limit = {
			original_tag = CHI
		}
		SIK = {
			OR = {
				exists = no
				is_subject_of = CHI
			}
		}
		XSM = {
			OR = {
				exists = no
				is_subject_of = CHI
			}
		}
		PRC = {
			OR = {
				exists = no
				is_subject_of = CHI
			}
		}
		SHX = {
			OR = {
				exists = no
				is_subject_of = CHI
			}
		}
		YUN = {
			OR = {
				exists = no
				is_subject_of = CHI
			}
		}
		GXC = {
			OR = {
				exists = no
				is_subject_of = CHI
			}
		}
	}
	if = {
		limit = {
			original_tag = PRC
		}
		SIK = {
			OR = {
				exists = no
				is_subject_of = PRC
			}
		}
		XSM = {
			OR = {
				exists = no
				is_subject_of = PRC
			}
		}
		CHI = {
			OR = {
				exists = no
				is_subject_of = PRC
			}
		}
		SHX = {
			OR = {
				exists = no
				is_subject_of = PRC
			}
		}
		YUN = {
			OR = {
				exists = no
				is_subject_of = PRC
			}
		}
		GXC = {
			OR = {
				exists = no
				is_subject_of = PRC
			}
		}
	}
	if = {
		limit = {
			original_tag = SIK
		}
		CHI = {
			OR = {
				exists = no
				is_subject_of = SIK
			}
		}
		XSM = {
			OR = {
				exists = no
				is_subject_of = SIK
			}
		}
		PRC = {
			OR = {
				exists = no
				is_subject_of = SIK
			}
		}
		SHX = {
			OR = {
				exists = no
				is_subject_of = SIK
			}
		}
		YUN = {
			OR = {
				exists = no
				is_subject_of = SIK
			}
		}
		GXC = {
			OR = {
				exists = no
				is_subject_of = SIK
			}
		}
	}
	if = {
		limit = {
			original_tag = XSM
		}
		CHI = {
			OR = {
				exists = no
				is_subject_of = XSM
			}
		}
		SIK = {
			OR = {
				exists = no
				is_subject_of = XSM
			}
		}
		PRC = {
			OR = {
				exists = no
				is_subject_of = XSM
			}
		}
		SHX = {
			OR = {
				exists = no
				is_subject_of = XSM
			}
		}
		YUN = {
			OR = {
				exists = no
				is_subject_of = XSM
			}
		}
		GXC = {
			OR = {
				exists = no
				is_subject_of = XSM
			}
		}
	}
	if = {
		limit = {
			original_tag = SHX
		}
		CHI = {
			OR = {
				exists = no
				is_subject_of = SHX
			}
		}
		SIK = {
			OR = {
				exists = no
				is_subject_of = SHX
			}
		}
		PRC = {
			OR = {
				exists = no
				is_subject_of = SHX
			}
		}
		XSM = {
			OR = {
				exists = no
				is_subject_of = SHX
			}
		}
		YUN = {
			OR = {
				exists = no
				is_subject_of = SHX
			}
		}
		GXC = {
			OR = {
				exists = no
				is_subject_of = SHX
			}
		}
	}
	if = {
		limit = {
			original_tag = YUN
		}
		CHI = {
			OR = {
				exists = no
				is_subject_of = YUN
			}
		}
		SIK = {
			OR = {
				exists = no
				is_subject_of = YUN
			}
		}
		PRC = {
			OR = {
				exists = no
				is_subject_of = YUN
			}
		}
		XSM = {
			OR = {
				exists = no
				is_subject_of = YUN
			}
		}
		SHX = {
			OR = {
				exists = no
				is_subject_of = YUN
			}
		}
		GXC = {
			OR = {
				exists = no
				is_subject_of = YUN
			}
		}
	}
	if = {
		limit = {
			original_tag = GXC
		}
		CHI = {
			OR = {
				exists = no
				is_subject_of = GXC
			}
		}
		SIK = {
			OR = {
				exists = no
				is_subject_of = GXC
			}
		}
		PRC = {
			OR = {
				exists = no
				is_subject_of = GXC
			}
		}
		XSM = {
			OR = {
				exists = no
				is_subject_of = GXC
			}
		}
		SHX = {
			OR = {
				exists = no
				is_subject_of = GXC
			}
		}
		YUN = {
			OR = {
				exists = no
				is_subject_of = GXC
			}
		}
	}
}

ai_strategy_check_if_has_no_war_with_GER_AND_ITA = {
	NOT = {
		has_war_with = GER
		has_war_with = ITA
	}
}

check_if_all_allies_are_ai = {
	ENG = {
		is_ai = yes
		all_allied_country = { is_ai = yes }
	}
	USA = { is_ai = yes }
}

check_if_cf154ever_crutches_enabled = {	#TODO
	OR = {
		has_global_flag = CF154EVER_DEV_SETTINGS_TRUE
	}
}

country_exists_AND_is_independent_from_ROOT = {
	exists = yes
	NOT = { is_subject_of = ROOT }
}

country_capitulated_or_lost_its_capital_to_ROOT = {
	OR = {
		has_capitulated = yes		
		capital_scope = {
			NOT = {
				is_controlled_by = PREV
			}
			is_controlled_by = ROOT
		}
	}
}

