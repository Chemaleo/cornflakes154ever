CF154EVER_germany_badass = {
	enable = {
		date > 1945.1.1		
		original_tag = GER
		has_government = fascism
		is_neighbor_of = SWE
		num_divisions > 200
		has_army_size = {
			size > 10
			type = armor
		}
		has_manpower > 100000
		OR = {
			has_equipment = {
				fighter_equipment > 5000
			}
			has_equipment = {
				CAS_equipment > 5000
			}
		}
		OR = {
			NOT = {
				has_war_with = USA
				has_war_with = ENG
				has_war_with = FRA
				has_war_with = SOV
			}
			AND = {
				16 = { controller = { is_in_faction_with = GER } }
				126 = { controller = { is_in_faction_with = GER } }
				195 = { controller = { is_in_faction_with = GER } }
				217 = { controller = { is_in_faction_with = GER } }
				219 = { controller = { is_in_faction_with = GER } }
			}
		}
		has_global_flag = cf154ever_enable_test_strategies
	}
	
	abort = {
		OR = {
			date < 1945.1.1
			NOT = { has_global_flag = cf154ever_enable_test_strategies }
		}
	}
	
	ai_strategy = {
		type = conquer
		id = SWE
		value = 99999
	}
	ai_strategy = {
		type = antagonize
		id = SWE
		value = 99999
	}
}